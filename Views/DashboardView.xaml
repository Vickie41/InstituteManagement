<UserControl x:Class="InstituteManagement.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Welcome Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,30">
                <TextBlock Text="Institute Dashboard" FontSize="28" FontWeight="Bold"/>
                <TextBlock Text="{Binding WelcomeMessage}" FontSize="16" Foreground="AliceBlue" 
                          Margin="0,5,0,0"/>
            </StackPanel>

            <!-- Key Metrics -->
            <ItemsControl Grid.Row="1" ItemsSource="{Binding KeyMetrics}" 
                         Margin="0,0,0,30">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" BorderBrush="#ddd" BorderThickness="1" 
                               CornerRadius="8" Margin="0,0,15,15" Padding="20,15">
                            <StackPanel>
                                <TextBlock Text="{Binding Value}" FontSize="24" FontWeight="Bold" 
                                          Foreground="#2196F3"/>
                                <TextBlock Text="{Binding Label}" FontSize="14" 
                                          Foreground="#666" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Recent Activity & Quick Actions -->
            <Grid Grid.Row="2" Margin="0,0,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Recent Activity -->
                <Border Grid.Column="0" Background="White" BorderBrush="#ddd" 
                       BorderThickness="1" CornerRadius="8" Padding="20">
                    <StackPanel>
                        <TextBlock Text="Recent Activity" FontSize="18" FontWeight="Bold" 
                                  Margin="0,0,0,15"/>
                        <ItemsControl ItemsSource="{Binding RecentActivities}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="10,8" BorderBrush="#eee" BorderThickness="0,0,0,1">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{Binding Icon}" 
                                                      FontSize="16" Margin="0,0,10,0"/>
                                            <TextBlock Grid.Column="1" Text="{Binding Description}" 
                                                      TextWrapping="Wrap"/>
                                            <TextBlock Grid.Column="2" Text="{Binding Time, StringFormat='{}{0:hh:mm tt}'}" 
                                                      Foreground="#666" FontSize="12"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- Quick Actions -->
                <Border Grid.Column="2" Background="White" BorderBrush="#ddd" 
                       BorderThickness="1" CornerRadius="8" Padding="20">
                    <StackPanel>
                        <TextBlock Text="Quick Actions" FontSize="18" FontWeight="Bold" 
                                  Margin="0,0,0,15"/>
                        <Button Content="➕ New Student" Command="{Binding AddStudentCommand}" 
                               Margin="0,5" Background="#4CAF50"/>
                        <Button Content="💰 Process Payment" Command="{Binding ProcessPaymentCommand}" 
                               Margin="0,5" Background="#2196F3"/>
                        <Button Content="📅 Take Attendance" Command="{Binding TakeAttendanceCommand}" 
                               Margin="0,5" Background="#FF9800"/>
                        <Button Content="📊 View Reports" Command="{Binding ViewReportsCommand}" 
                               Margin="0,5" Background="#9C27B0"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>