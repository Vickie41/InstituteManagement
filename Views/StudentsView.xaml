<UserControl x:Class="InstituteManagement.Views.StudentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Student Management" FontSize="24" FontWeight="Bold"/>
            <Button Content="+ Add New Student" Command="{Binding AddStudentCommand}" 
                    Margin="20,0,0,0" Background="#4CAF50"/>
        </StackPanel>

        <!-- Students Grid -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Students}" 
                  AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding StudentCode}" Width="100"/>
                <DataGridTextColumn Header="Name" Binding="{Binding FullNameEnglish}" Width="200"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding PhoneNumber}" Width="120"/>
                <DataGridTextColumn Header="Parent" Binding="{Binding ParentName}" Width="150"/>
                <DataGridTextColumn Header="Joined Date" 
                                   Binding="{Binding CreatedDate, StringFormat='dd-MMM-yyyy'}" Width="120"/>
                <DataGridTemplateColumn Header="Actions" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Edit" Command="{Binding DataContext.EditStudentCommand, 
                                        RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}" 
                                        Background="#FFC107" Foreground="Black" Padding="8,4" Margin="2"/>
                                <Button Content="Enroll" Command="{Binding DataContext.EnrollStudentCommand, 
                                        RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}" 
                                        Background="#2196F3" Padding="8,4" Margin="2"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>