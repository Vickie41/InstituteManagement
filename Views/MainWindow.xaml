<Window x:Class="InstituteManagement.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:common="clr-namespace:InstituteManagement.Common"
        Title="Hlaing Tharyar Skills Institute" 
        Height="768" Width="1024"
        WindowStartupLocation="CenterScreen"
        Background="#f5f5f5">

    <Window.Resources>
        <common:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="5,2"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#555"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2196F3" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="🏫" FontSize="24" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="Hlaing Tharyar Skills Institute" 
                          Foreground="White" FontSize="20" FontWeight="Bold" 
                          VerticalAlignment="Center" Margin="10,0"/>
                <TextBlock Grid.Column="2" Text="{Binding CurrentDateTime}" 
                          Foreground="White" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Navigation & Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Navigation Menu -->
            <Border Grid.Column="0" Background="#333" Padding="10">
                <StackPanel>
                    <Button Content="📊 Dashboard" 
                            Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateCommand}"
                            CommandParameter="Dashboard"/>

                    <Button Content="👥 Students" 
                            Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateCommand}"
                            CommandParameter="Students"/>

                    <Button Content="📚 Courses" 
                            Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateCommand}"
                            CommandParameter="Courses"/>

                    <Button Content="💰 Payments" 
                            Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateCommand}"
                            CommandParameter="Payments"/>

                    <Button Content="📅 Attendance" 
                            Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateCommand}"
                            CommandParameter="Attendance"/>

                    <Button Content="📈 Reports" 
                            Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateCommand}"
                            CommandParameter="Reports"/>
                </StackPanel>
            </Border>

            <!-- Main Content Area -->
            <ContentControl Grid.Column="1" Margin="20" Content="{Binding CurrentView}"/>
        </Grid>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#eee" Padding="10">
            <TextBlock Text="© 2024 Hlaing Tharyar Skills Institute. All rights reserved." 
                      HorizontalAlignment="Center" Foreground="#666"/>
        </Border>
    </Grid>
</Window>